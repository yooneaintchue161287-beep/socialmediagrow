export default async function handler(req, res) {
  // Example logic (replace later with OpenAI)
  res.json({
    message:
      "AI analyzed hashtags, timing, and niche. Growth campaign started.",
  });
}
